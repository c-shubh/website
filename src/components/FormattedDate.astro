---
import dayjs from 'dayjs';
interface Props {
	date: Date;
	class?: string;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class={Astro.props.class}>
	{dayjs(date).format('MMM DD, YYYY')}
</time>
