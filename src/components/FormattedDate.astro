---
import dayjs from 'dayjs';
interface Props {
	date: Date;
	class?: string;
}

const props = Astro.props;
const { date } = props;
---

<time datetime={date.toISOString()} class={props.class}>{dayjs(date).format('MMM DD, YYYY')}</time>
